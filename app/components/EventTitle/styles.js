import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 18,
    color: '#FFB400',
    textAlign: 'center',
    marginRight: 10,
    marginLeft: 10
  },
});

export default styles;
